# DG from mockup

## UI Layout & Behavior

- Header at the top.
- Section labeled "Add New Item" with an input field and an "Add" button.
- Section labeled "Items from Database" listing items (Item 1, Item 2, Item 3, etc.).
- For each item, three buttons labeled "P1", "P2", "P3" (priority options), and a "Delete" button.
- Priority buttons are grouped together horizontally for each item.

## Priority Button Behavior

- Priority buttons ("P1", "P2", "P3") act like radio buttons:
  - Only one can be selected at a time for each item.
- When unselected: Button is gray.
- When selected: Button is blue.

## Color Codes (from mockup)

- Gray: `#7A7A7A`
- Blue: `#07F2E6`

## Additional Notes

- No other colors specified for other UI elements (e.g., Delete button, header, etc.).
- No hover, focus, or disabled states specified for the buttons.
- No font, border radius, or spacing specified, except that the buttons are rectangular and grouped closely.
- No specification if the priority can be changed after creation, but radio button behavior suggests it can.

